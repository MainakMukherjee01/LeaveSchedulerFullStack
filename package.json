{
	"name": "leave-scheduler-root",
	"scripts": {
		"start:mysql": "brew services start mysql",
		"start:backend": "cd LeaveScheduler && mvn spring-boot:run -DSkipTests",
		"start:frontend": "cd leave-scheduler-frontend && npm run dev",
		"start:all": "run-p -s start:mysql start:backend start:frontend"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	}
}
